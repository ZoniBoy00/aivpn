# Normalize line endings across platforms
* text=auto

# Shell scripts MUST stay LF — CRLF breaks shebang/exec in Linux containers
*.sh text eol=lf
Dockerfile text eol=lf
Makefile text eol=lf
.gitignore text eol=lf
